# Local defaults work without this file. Set the deployed PartyKit host in each
# Cloudflare Pages project without the https:// prefix.
VITE_PARTYKIT_HOST=127.0.0.1:1999

# NBA builds set this through npm run dev:nba / build:nba.
# VITE_SPORT=nba
